apiVersion: v1
kind: Service
metadata:
  name: {{ include "gamania-dev.fullname" . }}
  labels:
    {{- include "gamania-dev.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gamania-dev.selectorLabels" . | nindent 4 }}
