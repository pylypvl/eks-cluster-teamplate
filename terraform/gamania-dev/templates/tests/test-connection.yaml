apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gamania-dev.fullname" . }}-test-connection"
  labels:
    {{- include "gamania-dev.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gamania-dev.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
